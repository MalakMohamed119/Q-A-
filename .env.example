# Set these values in Vercel Project Settings > Environment Variables.
# Never expose this key in browser JavaScript or commit a real .env file.
OPENAI_API_KEY=your_openai_api_key
# Optional: set a text model available to your OpenAI project.
OPENAI_MODEL=gpt-5-mini
